// Creo una variable con mi TOKEN para la api
const API_TOKEN = process.env.EXPO_PUBLIC_API_TOKEN;

// Guardo mi TOKEN dentro de una variable y la exporto
const KEYS = { API_TOKEN };

export default KEYS;